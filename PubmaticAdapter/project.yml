# PubmaticAdapter XcodeGen Project Specification
# This file uses the shared module template and adds PubmaticAdapter-specific overrides
# Template: Config/xcodegen/module.project_template.yml

include:
  - ../Config/xcodegen/module.project_template.yml

name: PubmaticAdapter

settings:
  base:
    # Product Configuration
    PRODUCT_NAME: PubmaticAdapter
    PRODUCT_BUNDLE_IDENTIFIER: msp.PubmaticAdapter
    PRODUCT_MODULE_NAME: PubmaticAdapter

targets:
  PubmaticAdapter:
    type: framework
    platform: iOS
    sources:
      - path: PubmaticAdapter
        excludes:
          - "**/*.md"
          - "**/Tests/**"
          - "**/*.test.swift"
          - "**/*.spec.swift"
          - "PubmaticAdapter.docc/**"
    settings:
      base:
        # Header Search Paths - inherit from CocoaPods xcconfig
        HEADER_SEARCH_PATHS: $(inherited)
        
        # CRITICAL: Override PODS_ROOT and PODS_PODFILE_DIR_PATH from xcconfig
        # xcconfig defines these relative to PubmaticAdapter/, but we need repo root
        PODS_ROOT: $(SRCROOT)/../Pods
        PODS_PODFILE_DIR_PATH: $(SRCROOT)/..
        
        # Framework Search Paths - Include $(PROJECT_DIR) to find local XCFrameworks
        # MSPiOSCore.xcframework and other frameworks from MSPSharedLibraries
        FRAMEWORK_SEARCH_PATHS: $(inherited) $(PROJECT_DIR)
    configFiles:
      Debug: "../Pods/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter.debug.xcconfig"
      Release: "../Pods/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter.release.xcconfig"
    prebuildScripts:
      - name: "[CP] Check Pods Manifest.lock"
        script: |
          if [ -z "${PODS_PODFILE_DIR_PATH}" ] || [ -z "${PODS_ROOT}" ]; then
            echo "error: CocoaPods environment variables are not set." >&2
            exit 1
          fi
          if [ ! -e "${PODS_ROOT}/Manifest.lock" ] || [ ! -e "${PODS_PODFILE_DIR_PATH}/Podfile.lock" ]; then
            echo "error: Run 'pod install' to generate Pods/Manifest.lock." >&2
            exit 1
          fi
          if ! diff "${PODS_PODFILE_DIR_PATH}/Podfile.lock" "${PODS_ROOT}/Manifest.lock" >/dev/null; then
            echo "error: The sandbox is not in sync with the Podfile.lock. Run 'pod install' or update your CocoaPods installation." >&2
            exit 1
          fi
        shell: /bin/sh
        inputFiles:
          - ${PODS_PODFILE_DIR_PATH}/Podfile.lock
          - ${PODS_ROOT}/Manifest.lock
    postbuildScripts:
      - name: "[CP] Copy Pods Resources"
        script: |
          if [ -f "${PODS_ROOT}/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter-resources.sh" ]; then
            "${PODS_ROOT}/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter-resources.sh"
          fi
        shell: /bin/sh
      - name: "[CP] Embed Pods Frameworks"
        script: |
          if [ -f "${PODS_ROOT}/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter-frameworks.sh" ]; then
            "${PODS_ROOT}/Target Support Files/Pods-PubmaticAdapter/Pods-PubmaticAdapter-frameworks.sh"
          fi
        shell: /bin/sh

schemes:
  PubmaticAdapter:
    build:
      targets:
        PubmaticAdapter: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

