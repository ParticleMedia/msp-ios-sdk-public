import Foundation

/// In some cases, if action key is enough and we don't need any more action data model.
/// we can use this as an empty place holder
public struct EmptyActionDataModel {
    public init() {

    }
}
