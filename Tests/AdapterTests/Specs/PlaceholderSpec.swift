import Quick
import Nimble

class AdapterPlaceholderSpec: QuickSpec {
    override class func spec() {
        describe("Adapter Test Target") {
            it("is properly configured") {
                expect(true).to(beTrue())
            }
        }
    }
}
