import Quick
import Nimble

class MSPiOSCorePlaceholderSpec: QuickSpec {
    override class func spec() {
        describe("MSPiOSCore Test Target") {
            it("is properly configured") {
                expect(true).to(beTrue())
            }
        }
    }
}
