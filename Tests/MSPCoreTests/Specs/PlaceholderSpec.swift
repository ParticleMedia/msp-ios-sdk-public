import Quick
import Nimble

class MSPCorePlaceholderSpec: QuickSpec {
    override class func spec() {
        describe("MSPCore Test Target") {
            it("is properly configured") {
                expect(true).to(beTrue())
            }
        }
    }
}
