import Nimble
import Quick

class NovaCorePlaceholderSpec: QuickSpec {
    override class func spec() {
        describe("NovaCore Test Target") {
            it("is properly configured") {
                expect(true).to(beTrue())
            }
        }
    }
}
