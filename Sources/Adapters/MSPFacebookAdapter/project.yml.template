# MSPFacebookAdapter XcodeGen Project Specification
# Uses shared template and adds MSPFacebookAdapter-specific overrides
# Template: Config/xcodegen/module.project_template.yml

include:
  - ../../../Configs/xcodegen/module.project_template.yml

name: MSPFacebookAdapter

settings:
  base:
    # Product Configuration
    PRODUCT_NAME: MSPFacebookAdapter
    PRODUCT_BUNDLE_IDENTIFIER: msp.MSPFacebookAdapter
    PRODUCT_MODULE_NAME: MSPFacebookAdapter

targets:
  MSPFacebookAdapter:
    type: framework
    platform: iOS
    sources:
      - path: MSPFacebookAdapter
        excludes:
          - "**/*.h"
          - "**/*.modulemap"
          - "**/*.md"
          - "**/Tests/**"
          - "**/*.test.swift"
          - "**/*.spec.swift"
          - "FacebookAdapter.docc/**"
    dependencies:
      - framework: ../../../Build/ReleaseArtifacts/XCFrameworks/MSPiOSCore.xcframework
        embed: false
      - framework: ../../../Build/ReleaseArtifacts/XCFrameworks/PrebidMobile.xcframework
        embed: false
    settings:
      base:
        BUILD_LIBRARY_FOR_DISTRIBUTION: YES
        SKIP_INSTALL: NO
        MACH_O_TYPE: mh_dylib
        
        # Framework Search Paths - XCFrameworks + FBAudienceNetwork shim
        FRAMEWORK_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/../../../Build/ReleaseArtifacts/XCFrameworks
          - $(SRCROOT)/../../Core/MSPSharedLibraries
          - $(SRCROOT)/../../../Sources/Core/MSPOMSDK/OMSDK_Newsbreak1.xcframework
          - $(SRCROOT)/../../../Sources/Wrappers/FBAudienceNetworkShim

        # Allow undefined symbols; App must link FBAudienceNetwork at runtime
        OTHER_LDFLAGS: $(inherited) -undefined dynamic_lookup

schemes:
  MSPFacebookAdapter:
    build:
      targets:
        MSPFacebookAdapter: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
