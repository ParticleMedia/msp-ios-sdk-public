# UnityAdapter XcodeGen Project Specification
# Uses shared template and adds UnityAdapter-specific overrides
# Template: Config/xcodegen/module.project_template.yml

include:
  - ../../../Configs/xcodegen/module.project_template.yml

name: UnityAdapter

settings:
  base:
    # Product Configuration
    PRODUCT_NAME: UnityAdapter
    PRODUCT_BUNDLE_IDENTIFIER: msp.UnityAdapter
    PRODUCT_MODULE_NAME: UnityAdapter

targets:
  UnityAdapter:
    type: framework
    platform: iOS
    sources:
      - path: UnityAdapter
        excludes:
          - "**/*.h"
          - "**/*.modulemap"
          - "**/*.md"
          - "**/Tests/**"
          - "**/*.test.swift"
          - "**/*.spec.swift"
          - "UnityAdapter.docc/**"
    dependencies:
      - framework: ../../../Build/ReleaseArtifacts/XCFrameworks/MSPiOSCore.xcframework
        embed: false
      - framework: ../../../Build/ReleaseArtifacts/XCFrameworks/MSPSharedLibraries.xcframework
        embed: false
      - framework: ../../../Pods/IronSourceSDK/IronSource/IronSource.xcframework
        embed: false
    settings:
      base:
        BUILD_LIBRARY_FOR_DISTRIBUTION: YES
        SKIP_INSTALL: NO
        
        # Framework Search Paths - XCFrameworks only
        FRAMEWORK_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/../../../Build/ReleaseArtifacts/XCFrameworks
          - $(SRCROOT)/../../Core/MSPSharedLibraries
          - $(SRCROOT)/../../../Sources/Core/MSPOMSDK/OMSDK_Newsbreak1.xcframework
          - $(SRCROOT)/../../../Pods/IronSourceSDK/IronSource

schemes:
  UnityAdapter:
    build:
      targets:
        UnityAdapter: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
